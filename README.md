This is an R-project that contains course materials for EDS 230/ESM 232
Materials are organized in directories for quarto lectures (lectures_qmd), papers, 
data and R (for R functions as part of the course), assignments 

Materials are updated before each lecture


Pull Changes with 

```
git fetch upstream
git merge upstream
git add .
git commit -m "merge upstream"
git push
```
